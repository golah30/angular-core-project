import { ForDifDirective } from './for-dif.directive';

describe('ForDifDirective', () => {
  it('should create an instance', () => {
    const directive = new ForDifDirective();
    expect(directive).toBeTruthy();
  });
});
