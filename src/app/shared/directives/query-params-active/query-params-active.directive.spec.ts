import { QueryParamsActiveDirective } from './query-params-active.directive';

describe('QueryParamsActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryParamsActiveDirective();
    expect(directive).toBeTruthy();
  });
});
