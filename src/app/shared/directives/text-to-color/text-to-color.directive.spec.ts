import { TextToColorDirective } from './text-to-color.directive';

describe('TextToColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToColorDirective();
    expect(directive).toBeTruthy();
  });
});
