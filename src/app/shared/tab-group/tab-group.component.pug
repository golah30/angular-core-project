ul.tab__list
    li.tab__item(
        *ngFor='let tab of tabs' 
        '[ngClass]'='{active: tab.active}' 
        '(click)'='selectTab(tab)'
    )
        a.tab__button('[routerLink]'="[{ outlets: { aside: [ tab.link ] } }]") {{tab.title}}
ng-content